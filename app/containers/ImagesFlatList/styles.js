import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  itemContainer: {
    flex: 1,
    flexDirection: 'row',
  },
  itemDescriptionContainer: {
    width: '100%',
  },
});

export default styles;

import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  itemContainer: {
    flex: 1,
    flexDirection: 'row',
  },
  itemDescriptionContainer: {
    paddingLeft: 5,
    justifyContent: 'center',
  },
});

export default styles;

import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  itemSeperator: {
    height: 1,
    alignSelf: 'stretch',
    backgroundColor: 'black',
  },
});

export default styles;
